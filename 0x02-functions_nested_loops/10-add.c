#include "main.h"

/**
 * add- adds two integers and returns the result
 *@n:number 1
 *@m:number 2
 * Return: the sum of two numbers
 */
int add(int n, int m)
{
	int sum;

	sum = n + m;
	return (sum);
}
